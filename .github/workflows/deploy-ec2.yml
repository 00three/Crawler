name: Deploy Crawler to EC2

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy-crawler-ec2
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Configure SSH
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/ec2_key
          chmod 600 ~/.ssh/ec2_key
          ssh-keyscan -H "${{ secrets.EC2_HOST }}" >> ~/.ssh/known_hosts

      - name: Deploy crawler service
        run: |
          set -euo pipefail
          ssh -i ~/.ssh/ec2_key "${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}" <<'REMOTE'
            set -euo pipefail
            cd /opt/ai-breaking-news/Crawler
            git fetch origin main
            git checkout main
            git pull --ff-only origin main
            cd /opt/ai-breaking-news/Full-Stack
            sudo docker compose up -d --build crawler
            sudo docker compose ps crawler
            sudo docker image prune -f
          REMOTE
